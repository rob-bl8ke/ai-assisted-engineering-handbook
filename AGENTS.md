## Agent skills

### System map

A walkable graph of every noun and process in this repo lives at `map/CLAUDE.md`. Read it before making structural changes. It routes to object cards (what each concept is and what a change hits) and process cards (the three repeating workflows).

### Issue tracker

Issues and specs for this repo live in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repo: read root `CONTEXT.md` and root `docs/adr/` when they exist. See `docs/agents/domain.md`.
